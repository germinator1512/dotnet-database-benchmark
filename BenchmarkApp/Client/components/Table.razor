@using BenchmarkApp.Shared

<table class="table">
    <thead>
    <tr>
        <th>Level</th>
        <th>LoadedEntities</th>
        <th>TimeSpan</th>
        <th>Success</th>
    </tr>
    </thead>
    <tbody>
    @foreach (var result in Results)
    {
        <tr>
            <td>@result.Level</td>
            <td>@result.LoadedEntities</td>
            <td>@result.MilliSeconds Milliseconds</td>
            <td style="@GetStyle(@result)">@result.Success</td>
        </tr>
    }
    </tbody>
</table>

@code {

    [Parameter]
    public IEnumerable<BenchmarkResult> Results { get; set; }

    private string GetStyle(BenchmarkResult result) =>
        result.Success ? "color:green" : "color:red";

}